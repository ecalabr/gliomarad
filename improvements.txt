implement integration of - move all tumor segmentation to the end so the model doesnt have to be loaded each time (attempted)
incorporate topup DTI files when/where they exist
make nets for image segmentation and synthetic CT
still issues with numbering keeper series, 11273473


loss masking
understand tf.reuse
image padding to divisible dimension
implement zero padding function to data input_fn
see if tf.py_func will pass params class, and if so pass this through to the lower level data IO functions
consider making one data loading function with optional augmentation