metrics choices for training validation
- param file? consider MAE, MSE, MAPE

Eventually maybe add custom evaluation between epochs?

https://github.com/tensorflow/tensorflow/issues/35982
model.fit not working (crashes) with variable length epochs, workaround with epoch for loop
eventually would be nice to get model.fit working with variable length epochs so steps_per_epoch does not neet to be specified

test nonzero weighted pixel loss

histogram matching of input images as a normalization method?

brain and tumor combined mask
